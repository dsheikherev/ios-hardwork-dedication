# Reuse reserved keywords in your code 
##

* If you want to name your variables with reserved keywords then surround it with backticks.

For example:
```
let `default` = 5
print(`default`)

let `class` = 5
print(`class`)
```